{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd app && ls && cd frontend && npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}
